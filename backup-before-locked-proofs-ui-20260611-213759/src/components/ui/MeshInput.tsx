import React from "react";
import { TextInput, TextInputProps } from "react-native";

export function MeshInput(props: TextInputProps) {
  return <TextInput {...props} />;
}

export default MeshInput;
